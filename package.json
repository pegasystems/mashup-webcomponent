{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "cd packages/core && npm test",
    "lint": "lerna run lint"
  },
  "workspaces": [
    "packages/*"
  ]
}