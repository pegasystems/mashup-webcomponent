{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@babel/preset-env": "^7.14.5",
    "jest": "^27.0.4",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "jest",
    "lint": "lerna run lint"
  },
  "workspaces": [
    "packages/*"
  ]
}
