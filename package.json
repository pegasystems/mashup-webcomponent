{
  "name": "root",
  "private": true,
  "devDependencies": {
    "jest": "^27.0.4",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "jest",
    "lint": "lerna run lint"
  },
  "workspaces": [
    "packages/*"
  ]
}
